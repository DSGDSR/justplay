export enum IGDBEndpoints {
    Game = 'games',
    Search = 'search'
}

export enum Endpoints {
    // GAMES
    GameById = '/games/:id',
    GameBySlug = '/games/slug/:slug',
    SearchGames = '/games/search',
}