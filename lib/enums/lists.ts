export enum ListType {
    Favorite = 1,
    Playlist = 2,
    Finished = 3,
    Custom = 4
}

export enum ListState {
    Active = 1,
    Inactive = 2,
    Loading = 3
}

export enum ListAction {
    Add = 1,
    Remove = 2
}