import { ListTypes } from './lists.enum'

export enum ActivityTypes {
    Favorite = ListTypes.Favorite,
    Playlist = ListTypes.Playlist,
    Finished = ListTypes.Finished,
    Custom = ListTypes.Custom
}