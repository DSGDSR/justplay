export enum IGDBEndpoints {
    Game = 'games',
    Covers = 'covers'
}

export enum Endpoints {
    // GAMES
    GameById = '/games/:id',
    GameBySlug = '/games/slug/:id',
    SearchGames = '/games/search',
}